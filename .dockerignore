# ────────────────────────────────────────────────
# ECOMMDEV — Docker ignore file
# Keeps image lean by excluding dev/build artifacts
# ────────────────────────────────────────────────

# Version control
.git
.gitignore

# Python bytecode / cache
__pycache__
*.py[cod]
*$py.class
*.so
.Python

# Build artifacts
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# Testing / Coverage
.coverage
htmlcov/
.tox/
.pytest_cache/
.mypy_cache/
coverage.xml

# Docs
docs/_build/

# Environments (never bake secrets into image)
.env
.env.local
.env.production

# Databases (local dev only)
*.sqlite3
*.db

# Media (mounted at runtime via volume)
media/
!media/.gitkeep

# Static (generated at build time via collectstatic)
staticfiles/

# Logs
*.log
logs/

# Node modules
node_modules/

# Docker meta (don't recurse into itself)
Dockerfile*
docker-compose*
.docker/

# CI/CD config (not needed inside image)
.github/
.gitlab-ci.yml
.travis.yml

# Documentation
*.md
!README.md

# Project meta (not needed in container)
LICENSE
Makefile
Pipfile
Pipfile.lock

# One-off scripts
add_*.py
fix_*.py
*_data.json

# Test fixtures
fixtures/
